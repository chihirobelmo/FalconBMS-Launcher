<Controls:MetroWindow x:Class="FalconBMS.Launcher.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        mc:Ignorable="d"
        Title="Login" Height="220" Width="380"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Email" VerticalAlignment="Center" Margin="0,0,8,8"/>
        <TextBox x:Name="EmailBox" Grid.Column="1" Margin="0,0,0,8"/>

        <TextBlock Text="Password" Grid.Row="1" VerticalAlignment="Center" Margin="0,0,8,8"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,8"/>

        <CheckBox x:Name="RememberMeBox" Grid.Row="2" Grid.ColumnSpan="2" Content="Remember me"/>

        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <TextBlock x:Name="StatusText" VerticalAlignment="Center" Margin="0,0,12,0"/>
            <Button Content="Cancel" Width="80" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button x:Name="LoginButton" Content="Login" Width="100" Click="Login_Click"/>
        </StackPanel>
    </Grid>
</Controls:MetroWindow>
